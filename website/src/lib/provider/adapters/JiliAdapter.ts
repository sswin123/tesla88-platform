import { BaseProviderAdapter } from '../ProviderAdapter';

// JILI Games adapter — placeholder.
// Implement verifySignature() and handle() when JILI API credentials are available.
export class JiliAdapter extends BaseProviderAdapter {
  readonly name = 'JILI';
}
